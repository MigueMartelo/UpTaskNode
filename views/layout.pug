<!DOCTYPE html>
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    meta(http-equiv="X-UA-Compatible", content="ie=edge")
    link(href="/css/normalize.css", rel="stylesheet")
    link(href="/css/app.css", rel="stylesheet")    
    title #{nombrePagina}
  body
    header.barra
      h1 UpTask - Administración de Proyectos
      a(href="#") Cerrar Sesión

    main.contenedor
      aside.contenedor-proyectos
        .panel.crear-proyecto
          a(href="/nuevo-proyecto", class="boton") Nuevo Proyecto
            span +
        .panel.lista-proyectos
          h2 Proyectos
          ul#proyectos.proyectos
            if(proyectos.length)
              each proyecto in proyectos
                li
                  a(href=`/proyectos/${proyecto.url}`)= proyecto.nombre
            else
              li No hay proyectos disponibles
      
      block contenido

      //- pre= vardump(proyectos)